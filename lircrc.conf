begin
  button = KEY_CHANNELDOWN
  prog = mopidy_controller
  config = KEY_CHANNELDOWN
end
begin
  button = KEY_HOME
  prog = mopidy_controller
  config = KEY_HOME
end
begin
  button = KEY_CHANNELUP
  prog = mopidy_controller
  config = KEY_CHANNELUP
end
begin
  button = KEY_PREVIOUS
  prog = mopidy_controller
  config = KEY_PREVIOUS
end
begin
  button = KEY_NEXT
  prog = mopidy_controller
  config = KEY_NEXT
end
begin
  button = KEY_PLAYPAUSE
  prog = mopidy_controller
  config = KEY_PLAYPAUSE
end
begin
  button = KEY_VOLUMEDOWN
  prog = mopidy_controller
  config = KEY_VOLUMEDOWN
end
begin
  button = KEY_VOLUMEUP
  prog = mopidy_controller
  config = KEY_VOLUMEUP
end
begin
  button = KEY_MODE
  prog = mopidy_controller
  config = KEY_MODE
end
begin
  button = KEY_F1
  prog = mopidy_controller
  config = KEY_F1
end
begin
  button = KEY_F2
  prog = mopidy_controller
  config = KEY_F2
end
begin
  button = KEY_0
  prog = mopidy_controller
  config = KEY_0
end
begin
  button = KEY_1
  prog = mopidy_controller
  config = KEY_1
end
begin
  button = KEY_2
  prog = mopidy_controller
  config = KEY_2
end
begin
  button = KEY_3
  prog = mopidy_controller
  config = KEY_3
end
begin
  button = KEY_4
  prog = mopidy_controller
  config = KEY_4
end
begin
  button = KEY_5
  prog = mopidy_controller
  config = KEY_5
end
begin
  button = KEY_6
  prog = mopidy_controller
  config = KEY_6
end
begin
  button = KEY_7
  prog = mopidy_controller
  config = KEY_7
end
begin
  button = KEY_8
  prog = mopidy_controller
  config = KEY_8
end
begin
  button = KEY_9
  prog = mopidy_controller
  config = KEY_9
end